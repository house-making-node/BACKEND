// package.json

"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon --exec babel-node index.js",
    "start:dist": "webpack --mode production & node ./dist/main.js", // 빌드 하고 빌드 파일 실행시켜보기
    "start": "node main.js",  // 배포 시 이 명령어로 실행이 됩니다!
  },